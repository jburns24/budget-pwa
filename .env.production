VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# VITE_USE_FAKE_AUTH and VITE_USE_FAKE_DATA intentionally absent — DEV guard in
# registry.ts ensures fake adapters are tree-shaken from production bundles regardless.
